@using ErkinStudy.Infrastructure.Services
@model NewOrderViewModel
@inject FolderService FolderService
@{
    ViewData["Title"] = "New Order";
}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h4 class="mb-3">New order</h4>
            <form method="post" asp-action="CreateOrder" novalidate>
                <div class="mb-3">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" name="email" id="email" value="@Model.Email" required="">
                </div>

                <div class="mb-3">
                    <label for="address">Phone Number</label>
                    <input type="text" class="form-control" name="address" id="address" value="@Model.PhoneNumber" required>
                </div>

                <input type="hidden" name="folderId" value="@Model.FolderId" required>
                <input type="hidden" name="amount" value="@Model.Amount" required>
                <div class="mb-4">
                    <ul class="list-group mb-3">
                        <li class="list-group-item d-flex justify-content-between">
                            <div>
                                <h6 class="my-0">@FolderService.GetFolderName(@Model.FolderId)</h6>
                                @*<small class="text-muted">Brief description</small>*@
                            </div>
                            <span class="text-muted">@Model.Amount т</span>
                        </li>
                    </ul>
                    @*<form class="card p-2">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Promo code">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-secondary">Redeem</button>
                    </div>
                </div>
            </form>*@
                </div>
                <hr class="mb-4">
                <button class="btn btn-primary btn-lg btn-block" type="submit">Continue to checkout</button>
            </form>
        </div>

    </div>
</div>