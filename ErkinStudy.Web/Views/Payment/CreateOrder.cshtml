@using ErkinStudy.Infrastructure.Services
@model ErkinStudy.Domain.Entities.Payment.Order
@inject FolderService FolderService
@{
    ViewData["Title"] = "CreateOrder";
}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h4 class="mb-3">Create order</h4>
            <form class="" novalidate>
                <div class="mb-3">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" id="email" value="@Model.Email" required="">
                </div>

                <div class="mb-3">
                    <label for="address">Phone Number</label>
                    <input type="text" class="form-control" id="address" value="@Model.PhoneNumber" required>
                </div>


                <div class="mb-4">
                    <ul class="list-group mb-3">
                        <li class="list-group-item d-flex justify-content-between">
                            <div>
                                <h6 class="my-0">@FolderService.GetFolderName(@Model.FolderId)</h6>
                                @*<small class="text-muted">Brief description</small>*@
                            </div>
                            <span class="text-muted">@Model.Amount т</span>
                        </li>
                    </ul>
                    @*<form class="card p-2">
                        <div class="input-group">
                            <input type="text" class="form-control" placeholder="Promo code">
                            <div class="input-group-append">
                                <button type="submit" class="btn btn-secondary">Redeem</button>
                            </div>
                        </div>
                    </form>*@
                </div>
                <hr class="mb-4">
                <button class="btn btn-primary btn-lg btn-block" type="submit">Continue to checkout</button>
            </form>
        </div>

    </div>
</div>